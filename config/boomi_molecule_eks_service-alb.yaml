apiVersion: v1
kind: Service
metadata:
  name: molecule-service-alb
  labels:
    app: molecule
spec:
  selector:
    app: molecule
  type: NodePort
  ports:
  - protocol: TCP
    port: 9090
    targetPort: 443
